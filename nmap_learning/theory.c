/*
узнать свою сеть
    ip a

Ping-сканирование (обнаружение хостов)
    nmap -sn 192.168.1.0/24

    -sn — без сканирования портов, только «кто жив».
    Вывод покажет IP и MAC-адреса активных устройств.

сканирование портов
    nmap 192.168.1.1

    По умолчанию Nmap сканирует 1000 наиболее популярных TCP-портов.
    Что смотреть в выводе:
        PORT STATE SERVICE
        22/tcp open ssh — значит, на роутере или устройстве открыт SSH.

Сканирование конкретных портов
    nmap -p 22,80,443 192.168.1.1
    nmap -p 1-1000 192.168.1.1
    nmap -p- 192.168.1.1                -> все 65535 портов

Определение версий сервисов
    sudo nmap -sV 192.168.1.1

    -sV — version detection. Nmap отправляет пробные запросы и сравнивает ответы с базой.
    Требует sudo, потому что использует сырые сокеты.

    там чето непонятное выводит

Определение ОС
    sudo nmap -O 192.168.1.1

    -O — OS fingerprinting. Точность зависит от количества открытых/закрытых портов и наличия файрвола.

Комбинированный «агрессивный» скан
    sudo nmap -A 192.168.1.1

    -A = -sV -O -sC --traceroute.
    -sC — запуск дефолтных NSE-скриптов.
    то самый информативный, но и самый «шумный» режим

Типы сканирования — флаги
    -sT	    TCP connect	            Полное соединение, работает без root, легко логируется
    -sS	    TCP SYN (half-open)	    Стелс, требует root, не завершает handshake
    -sU	    UDP	                    Медленно, важно для DNS/SNMP/DHCP
    -sN	    NULL	                Все флаги выключены
    -sF	    FIN	                    Только FIN-флаг
    -sX	    Xmas	                FIN+PSH+URG

мини итог
    Анализ по 185.171.82.223

    1. nmap -sn 185.171.82.223 — хост жив, latency 8.7 мс. Ты в России (судя по трейсу через ertelecom), значит цель где-то в РФ, ~10 хопов.

    2. Базовый скан. Картина классическая для хоста за файрволом:

    22/tcp open ssh — OpenSSH 9.6p1 Ubuntu 3ubuntu13.19 (Ubuntu 24.04).
    80/tcp open http — Caddy httpd. Заголовок Bingo №Шаурма... (сервер работает как реверс-прокси, возможно, на какой-то внутренний сервис).
    443/tcp open ssl/https — TLS есть, но TLS randomness does not represent time — самоподписанный или нестандартный сертификат.
    4000/tcp open remoteanything? — Nmap не распознал, но в fingerprint-строках видно HTTP/1.1 204 No Content, RTSPRequest, куча security-заголовков (CSP, HSTS, X-Frame-Options). Это точно Caddy или похожий reverse-proxy, отвечающий на 4000.
    Порты 19, 111, 135, 139, 389, 445, 1434, 1900 — filtered. Это значит, что файрвол дропает пакеты (не отвечает RST, а просто игнорирует). Классическая картина: снаружи торчат только 22, 80, 443, 4000, остальное закрыто на уровне FW.
    988 closed (reset) — на остальные 988 портов из топ-1000 пришёл RST. То есть файрвол не «стелс», а конкретно фильтрует список.
    3. -O — не дал точного матча, потому что «test conditions non-ideal» (мало открытых/закрытых портов, файрвол мешает). Но уверенно говорит: Linux 4.19–6.12, 92–96%, плюс IPFire 2.27. Скорее всего — Ubuntu 24.04 LTS.

    4. -A — подтвердил всё выше + SSH-ключи (ECDSA + ED25519) + HTTP-заголовки.

    Network Distance: 10 hops.
    traceroute показал путь: 192.168.1.1 (твой роутер) → сети ertelecom.ru (176.213.135.x) → 31.131.196.169 → 10.255.3.19 → 10.66.33.147 → цель 185.171.82.223.
    5. -sT vs -sS. Обе команды дали идентичный результат по портам, но:

    -sT — соединение завершается полностью (conn-refused).
    -sS — полуоткрытое, файрвол отвечает reset.
    Разница в поведении только в «988 closed»: для -sT — conn-refused, для -sS — reset. По сути одно и то же, разные уровни TCP-стека.
    Вывод по цели: Это Linux-сервер за файрволом, вероятно, VPS с Caddy в роли reverse-proxy, отдающий что-то похожее на внутренний сервис («Шаурма»). SSH открыт наружу.



















*/